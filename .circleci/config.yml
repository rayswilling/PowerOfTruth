version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

      - run:
          name: update-npm
          command: npm install -g npm@latest

      - run: npm install

      - run:
          name: Print the Current Time
          command: date

      - run:
          name: Run tests
          command: npm test
