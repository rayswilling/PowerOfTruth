version: 2
jobs:
  build:
    docker:
      - image: debian:stretch

    steps:
      - checkout

    - run:
        name: Install npm
        command: brew install node

      - run: npm install

      - run:
          name: Print the Current Time
          command: date

      - run:
          name: Run tests
          command: npm test
